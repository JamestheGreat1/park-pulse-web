{
  "name": "parkpulse-worker",
  "version": "1.3.7",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "generate-vapid": "node ./scripts/generate-vapid.mjs"
  },
  "dependencies": {
    "web-push-neo": "^0.1.2"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
